# Soduto Android: Patch (Downstream) Manifest
#
# Single source of truth for this repository's CURRENT state relative to upstream.
# Full rebase history lives in SYNC_LOG.yaml, this file only tracks the present.
#
# Fields marked [auto] are updated by CI during sync/rebase workflows.
# All other fields are human-maintained.
#
# HOW TO USE DURING A REBASE CONFLICT:
#   1. Look up the conflicting file in the `patches` section below
#   2. Read its `intent` to understand WHY we changed it
#   3. Read its `resolution_strategy` for HOW to resolve
#   4. After resolving, update `conflicts` in SYNC_LOG.yaml
#
# FIELD FORMAT REFERENCE:
#   Dates    → "YYYY-MM-DD" (quoted, ISO 8601 date only)
#   Commits  → full 40-character SHA-1 hex string
#   Nulls    → use `null` (not "" or ~) for empty optional fields
#   Lists    → use `[]` for empty lists, never omit the key

# ─── Upstream Tracking ───────────────────────────────────────────────────────

upstream:
  # HTTPS URL of the canonical upstream repository.
  repository: https://invent.kde.org/network/kdeconnect-android.git

  # The upstream branch we track and rebase onto.
  branch: master

  # [auto] The upstream commit at the tip of upstream/master that our downstream
  # patches are currently rebased onto. This is NOT a downstream commit, it's
  # the exact commit from upstream's history. After a rebase, this becomes the
  # new upstream/master tip. Use this to answer: "how far behind upstream are we?"
  #
  #   git log <base_commit>..upstream/master --oneline   → pending upstream commits
  #   git rev-list --count <base_commit>..upstream/master → count of pending commits
  base_commit: ed2ba2074812482d185dc2aa4c83bf804c35912a

  # [auto] Date when upstream/* refs on GitHub were last synced from KDE Invent
  # by the sync-upstream workflow. This does NOT mean main was rebased, only
  # that the upstream/* mirror branches/tags are up to date as of this date.
  last_sync: "2026-03-29"

# ─── Downstream Identity ─────────────────────────────────────────────────────

downstream:
  name: Soduto
  application_id: com.thenoton.soduto
  website: https://soduto.thenoton.com
  repository: https://github.com/sannidhyaroy/soduto-android.git

# ─── Last Rebase ─────────────────────────────────────────────────────────────
# Snapshot of the most recent rebase of main onto upstream/master.
# Overwritten each rebase. See SYNC_LOG.yaml for the full history.
#
# The pre_rebase_head is the most important field here. It points to the
# orphaned commit that was main's HEAD before the rebase + force-push.
# Recording it here is a human-readable audit trail, it does NOT keep the
# object alive in git (reachability is determined by the object graph, not
# file contents). The orphaned commit survives locally for ~90 days (reflog)
# and on GitHub until GC. Use these hashes within that window:
#
#   git log <pre_rebase_head>                → inspect old branch state
#   git diff <pre_rebase_head>..main         → verify nothing was lost in rebase
#   git cherry-pick <hash>                   → recover a specific old commit
#   git checkout -b recovery <pre_rebase_head> → full recovery branch
#
# For permanent recovery: create a tag or archive ref pointing to pre_rebase_head BEFORE rebasing.

last_rebase:
  # Date the rebase was performed.
  date: "2026-03-29"

  # [auto] The value of upstream.base_commit BEFORE this rebase.
  # null on initial downstream creation (there was no previous base).
  previous_base: null

  # [auto] The upstream commit we rebased onto (becomes the new upstream.base_commit).
  new_base: ed2ba2074812482d185dc2aa4c83bf804c35912a

  # [auto] The commit hash that was HEAD of main before the rebase.
  # This commit becomes orphaned after force-push. Recorded here and in
  # SYNC_LOG.yaml as a human-readable audit trail.
  # null on initial downstream creation (main was created fresh, nothing to orphan).
  pre_rebase_head: null

  # [auto] The commit hash that is HEAD of main after the rebase.
  # Cross-reference with `git reflog main` to verify consistency.
  post_rebase_head: ed2ba2074812482d185dc2aa4c83bf804c35912a

  # [auto] Number of new upstream commits integrated in this rebase.
  #   git rev-list --count <previous_base>..<new_base>
  upstream_commits_integrated: 0

  # Files that had merge conflicts during this rebase. Empty list if clean.
  # Format: list of filenames, e.g. [build.gradle.kts, src/SomeFile.kt]
  conflicts: []

  # Downstream patches intentionally removed during this rebase because
  # upstream caught up or the change is no longer needed.
  # Format: list of objects with `file` and `reason` keys.
  # Example:
  #   - file: src/.../ClipboardPlugin.kt
  #     reason: Upstream merged equivalent fix in commit abc123
  dropped_patches: []

  # Free-form notes about this rebase. Anything notable like large upstream
  # refactors, tricky conflict resolutions, things to watch in the next sync.
  notes: Downstream created (main branched from upstream/master)

# ─── Patch Manifest ──────────────────────────────────────────────────────────
# Every file in our downstream that differs from upstream.
#
# Split into `modified` (upstream files we edited) and `added` (new files
# that don't exist upstream). Modified files are where rebase conflicts happen;
# added files never conflict.
#
# The file lists can be verified/regenerated from git:
#   git diff --name-status <base_commit>..main
#     M = modified, A = added
#
# REQUIRED FIELDS (every entry must have these):
#   file   → path relative to repo root
#   intent → one-line explanation of WHY this change exists (not what it does)
#
# OPTIONAL FIELDS (for modified files, fill these to make rebases painless):
#   conflict_prone       → true/false → does upstream frequently touch this file?
#   resolution_strategy  → how to resolve conflicts when they arise
#   upstream_issue       → URL to upstream bug/MR if they might fix this themselves
#   drop_when            → condition under which this patch becomes unnecessary
#                          and should be removed (e.g. "upstream merges MR !1234")

patches:
  modified:
    - file: COPYING
      intent: Re-licensed to GPL-3.0 for Soduto downstream
      conflict_prone: false
      resolution_strategy: >-
        Keep our GPL-3.0 text for the file content itself. We can do this because
        upstream's SPDX expression is "GPL-2.0-only OR GPL-3.0-only OR
        LicenseRef-KDE-Accepted-GPL", and we exercised the GPL-3.0 option.
        Upstream code arrives under the same multi-license and we receive them as
        GPL-3.0.
        HOWEVER, if upstream's COPYING itself changes during a rebase, do NOT
        auto-resolve. Stop and assess:
        - If they expanded options or became more permissive: keep ours, no issue.
        - If they dropped the GPL-3.0 option or became more restrictive (GPL-2.0-only,
          SSPL, Commons Clause, etc.): this is a legal incompatibility. New commits
          after that point CANNOT be received as GPL-3.0. The existing codebase up to
          the last compatible commit is still valid under GPL-3.0. Do not proceed with
          the rebase until the licensing situation is evaluated.
      upstream_issue: null
      drop_when: null

    - file: README.md
      intent: Complete rewrite for Soduto branding, documentation, and GPG release verification
      conflict_prone: false
      resolution_strategy: >-
        Keep ours. Our README is entirely Soduto-specific (branding, install
        links, signing key, limitations). Upstream changes to README.md are
        irrelevant. Always take our version.
      upstream_issue: null
      drop_when: null

    - file: build.gradle.kts
      intent: Override applicationId to com.thenoton.soduto
      conflict_prone: false
      resolution_strategy: >-
        One line changed in defaultConfig: applicationId = "com.thenoton.soduto".
        Upstream rarely touches applicationId specifically (only versionCode/versionName
        change frequently in defaultConfig, on separate lines). If a conflict occurs,
        keep our applicationId value and accept upstream's other defaultConfig changes.
      upstream_issue: null
      drop_when: null

    - file: fastlane/Appfile
      intent: Update package_name to com.thenoton.soduto for Play Store deployment
      conflict_prone: false
      resolution_strategy: Keep ours. Upstream uses org.kde.kdeconnect_tp, we use com.thenoton.soduto.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/xml/fileprovider_paths.xml
      intent: Fix file provider path to use Soduto's applicationId (was org.kde.kdeconnect_tp)
      conflict_prone: false
      resolution_strategy: >-
        Keep ours. The path Android/data/com.thenoton.soduto/files/Pictures must
        match our applicationId. If upstream changes this file, incorporate their
        structural changes but keep our package name in the path.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/plugins/share/CompositeReceiveFileJob.java
      intent: Use dynamic FileProvider authority (getPackageName() + ".fileprovider") instead of hardcoded org.kde.kdeconnect_tp
      conflict_prone: false
      resolution_strategy: Keep ours. This is a correctness fix upstream should have too.
      upstream_issue: null
      drop_when: Upstream fixes the hardcoded authority

    - file: src/main/java/org/kde/kdeconnect/plugins/notifications/NotificationsPlugin.java
      intent: >-
        Dynamic package name check to avoid forwarding our own notifications;
        delegate extractIcon to SodutoNotificationsHelper for improved icon hierarchy;
        register SharedPreferences listener on PREFS_ICON to clear notificationsIcons
        cache when icon settings change so icons are re-sent after user adjustments
      conflict_prone: false
      resolution_strategy: >-
        Three independent changes. Package name check and listener registration are
        always kept. The extractIcon delegation can be dropped if upstream provides
        an equivalent hierarchy — but the listener stays regardless because it is
        tied to notificationsIcons cache invalidation, not to extractIcon itself.
      upstream_issue: null
      drop_when: Upstream fixes the hardcoded package name check and improves extractIcon

    - file: src/main/java/org/kde/kdeconnect/plugins/systemvolume/SystemVolumePlugin.kt
      intent: Request sink list from desktop on connect so the UI populates immediately
      conflict_prone: false
      resolution_strategy: Keep ours. If upstream adds onCreate(), merge our requestSinks packet send into it.
      upstream_issue: null
      drop_when: Upstream adds the requestSinks call on connect

    - file: src/main/java/org/kde/kdeconnect/plugins/mprisreceiver/MprisReceiverPlayer.kt
      intent: >-
        Add seek(), getShuffle()/setShuffle(), getLoopStatus()/setLoopStatus() via
        MediaControllerCompat; expose shuffle and loopStatus state to the plugin
      conflict_prone: false
      resolution_strategy: >-
        Keep ours. If upstream adds similar capabilities, evaluate whether our
        MediaControllerCompat bridge is still needed or can be dropped.
      upstream_issue: null
      drop_when: Upstream adds seek/shuffle/loop support natively

    - file: src/main/java/org/kde/kdeconnect/plugins/mprisreceiver/MprisReceiverPlugin.java
      intent: >-
        Handle Seek, setShuffle, setLoopStatus incoming packets; include shuffle and
        loopStatus in outgoing metadata; register ContentObserver on
        Settings.System.CONTENT_URI to push metadata updates on stream volume changes
      conflict_prone: true
      resolution_strategy: >-
        Upstream actively develops this file. Our additions are in three
        independent areas: packet handling (onPacketReceived), metadata sending
        (sendMetadata), and volume tracking (onCreate/onDestroy). On conflict,
        reapply each block independently.
      upstream_issue: null
      drop_when: Upstream adds seek/shuffle/loop/volume-change support natively

    - file: src/main/java/org/kde/kdeconnect/plugins/mpris/MprisMediaSession.kt
      intent: Use BuildConfig.APPLICATION_ID for MediaSession tag instead of hardcoded package name
      conflict_prone: false
      resolution_strategy: Keep ours. This is a correctness fix upstream should have too.
      upstream_issue: null
      drop_when: Upstream fixes the hardcoded tag

    - file: src/main/java/org/kde/kdeconnect/plugins/runcommand/RunCommandWidgetConfigActivity.kt
      intent: Use BuildConfig.APPLICATION_ID for SharedPreferences name instead of hardcoded package name
      conflict_prone: false
      resolution_strategy: Keep ours. This is a correctness fix upstream should have too.
      upstream_issue: null
      drop_when: Upstream fixes the hardcoded prefs name

    - file: src/main/java/org/kde/kdeconnect/plugins/clipboard/ClipboardFloatingActivity.java
      intent: Update ADB command examples in comments to use Soduto package name
      conflict_prone: false
      resolution_strategy: Keep ours. Comment documentation only.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/plugins/clipboard/ClipboardListener.kt
      intent: >-
        Fix broken logcat filter syntax ("E ClipboardService" passed as a single
        space-containing argument is invalid — always use "ClipboardService:E");
        extract monitoring into startLogcatMonitoringIfNeeded() so it can be triggered
        without restarting the app when READ_LOGS is granted mid-session
      conflict_prone: true
      resolution_strategy: >-
        Keep our logcat filter fix and startLogcatMonitoringIfNeeded() extraction.
        If upstream changes the monitoring logic or adds version checks, reapply
        our filter fix and ensure startLogcatMonitoringIfNeeded() still integrates.
      upstream_issue: null
      drop_when: Upstream fixes the logcat filter syntax and adds a post-grant monitoring start

    - file: src/main/java/org/kde/kdeconnect/plugins/clipboard/ClipboardPlugin.kt
      intent: >-
        Fix inverted canAccessLogs() logic (was returning true on PERMISSION_DENIED);
        rename to hasReadLogsPermission() with correct PERMISSION_GRANTED semantics;
        call startLogcatMonitoringIfNeeded() on device connect to pick up a
        freshly granted READ_LOGS permission without restarting the app
      conflict_prone: false
      resolution_strategy: >-
        Keep ours. If upstream modifies getUiButtons()/getUiMenuEntries(), reapply
        our corrected hasReadLogsPermission() check with the correct polarity.
      upstream_issue: null
      drop_when: Upstream fixes the inverted canAccessLogs() logic

    - file: src/main/AndroidManifest.xml
      intent: Change app label to @string/app_name and SendFileActivity label to @string/app_name
      conflict_prone: true
      resolution_strategy: >-
        Upstream frequently touches the manifest. Our changes are two label attributes
        on <application> and SendFileActivity. On conflict, keep our label values and
        accept upstream's other manifest changes.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/values/strings.xml
      intent: Add app_name="Soduto" (untranslatable), override kde_connect and all user-visible KDE Connect references to Soduto
      conflict_prone: true
      resolution_strategy: >-
        Upstream adds strings here frequently. Accept new upstream strings, but keep
        our overridden values for kde_connect and app_name. Never accept upstream
        kde_connect value ("KDE Connect") — always keep ours ("Soduto").
      upstream_issue: null
      drop_when: null

    - file: src/debug/res/values/strings.xml
      intent: Override kde_connect to "Debug Soduto" for debug builds
      conflict_prone: false
      resolution_strategy: Keep ours.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/backends/bluetooth/BluetoothLinkProvider.kt
      intent: Change Bluetooth RFCOMM service record display name from "KDE Connect" to "Soduto"
      conflict_prone: false
      resolution_strategy: Keep ours. Display name only — UUID is unchanged so connectivity is unaffected.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/ui/SettingsFragment.kt
      intent: Change log export filename and file header from "KDE Connect" to "Soduto"
      conflict_prone: false
      resolution_strategy: Keep ours. Two-line change — if upstream modifies this file, reapply our strings.
      upstream_issue: null
      drop_when: null

    - file: settings.gradle.kts
      intent: Change rootProject.name to "soduto-android" so APK filename reflects the app name
      conflict_prone: false
      resolution_strategy: Keep ours. One-line change.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/KdeConnect.kt
      intent: Change all log tags and HandroidLoggerAdapter.APP_NAME from "KDEConnect" to "Soduto"
      conflict_prone: false
      resolution_strategy: Keep ours. If upstream modifies this file, reapply our tag strings.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/helpers/TrustedDevices.kt
      intent: Change log tag from "TrustedDevices" to "Soduto/TrustedDevices"
      conflict_prone: false
      resolution_strategy: Keep ours. One-line change.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/ui/about/ApplicationAboutData.kt
      intent: Add Sannidhya Roy as second author entry; update Albert's task string
      conflict_prone: false
      resolution_strategy: Keep ours. If upstream adds more authors, preserve our Sannidhya Roy entry after Albert.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/ui/about/EasterEggActivity.kt
      intent: Change Easter Egg background color from KDE blue to Soduto green; add white color filter on logo
      conflict_prone: false
      resolution_strategy: Keep ours. Always take our version on conflict.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/layout/activity_easter_egg.xml
      intent: Change Easter Egg background to Soduto green and icon to ic_launcher_foreground
      conflict_prone: false
      resolution_strategy: Keep ours. Always take our version on conflict.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/plugins/notifications/NotificationFilterActivity.java
      intent: >-
        Add notification sync settings title; instantiate SodutoNotificationFilterController
        to own the notification icon two-target row and bottom sheet without bloating
        the upstream file
      conflict_prone: true
      resolution_strategy: >-
        Upstream has a TODO to convert this to a PluginSettingsFragment — if that
        happens, migrate our two call-site lines (controller instantiation + configure)
        into the replacement class. The controller itself lives in our file and needs
        no changes unless the binding type changes.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/layout/activity_notification_filter.xml
      intent: >-
        Add notification icon two-target row (master toggle + tap-to-configure) above
        the app filter list; replace plain TextView with MaterialTextView for filter
        description; add Notification Filter section header and divider; replace
        ProgressBar with Material 3 Expressive LoadingIndicator
      conflict_prone: true
      resolution_strategy: >-
        Our additions are a self-contained block at the top of the LinearLayout and a
        section header above the existing tFilter view. If upstream restructures the
        layout, reapply our block before smScreenOffNotification and our section header
        above tFilter. The tFilter id and smScreenOffNotification id must remain intact.
      upstream_issue: null
      drop_when: null

    - file: src/main/java/org/kde/kdeconnect/plugins/remotekeyboard/RemoteKeyboardService.java
      intent: Replace deprecated KeyboardView/Keyboard/OnKeyboardActionListener with SodutoRemoteKeyboardView delegation
      conflict_prone: false
      resolution_strategy: >-
        Field type and two method bodies changed (onCreateInputView, updateInputView).
        If upstream refactors these methods, evaluate whether SodutoRemoteKeyboardView
        still integrates correctly and update the call sites.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/layout/widget_remotecommandplugin.xml
      intent: Replace KDE Connect icon with Soduto foreground in widget title bar; fix ImageView size
      conflict_prone: false
      resolution_strategy: Keep ours. If upstream changes this layout, reapply our icon and dimensions.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/layout/fragment_about.xml
      intent: Use ShapeableImageView with rounded corners for About page icon; fix vertical alignment and icon/text spacing
      conflict_prone: false
      resolution_strategy: Keep ours. If upstream changes this layout, reapply our ShapeableImageView changes.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/values/styles.xml
      intent: Add ShapeAppearanceOverlay.AppIcon style (11dp rounded corners) for About page icon
      conflict_prone: false
      resolution_strategy: Keep ours. If upstream adds styles, incorporate them and keep our overlay style.
      upstream_issue: null
      drop_when: null

    - file: "src/main/res/values-{de,fr,ja,ko,ru,zh-rCN}/strings.xml"
      intent: Soduto-branded string overrides for the 6 supported locales
      conflict_prone: false
      resolution_strategy: Accept upstream additions; keep our overridden Soduto string values.
      upstream_issue: null
      drop_when: null

    - file: "fastlane/metadata/android/{en-US,de-DE,fr-FR,ja-JP,ko-KR,ru-RU,zh_CN}/"
      intent: Rewritten Soduto store listing (en-US) and translated for 6 languages
      conflict_prone: false
      resolution_strategy: Keep ours. Complete Soduto-specific content.
      upstream_issue: null
      drop_when: null

    - file: "{icon,adaptive_icon}.svg"
      intent: Replace KDE Connect icon sources with Soduto artwork
      conflict_prone: false
      resolution_strategy: Keep ours. Complete replacements. Always take our version.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/drawable-v24/ic_launcher_background.xml
      intent: Replace KDE Connect launcher background with Soduto green diagonal gradient
      conflict_prone: false
      resolution_strategy: Keep ours. Complete replacement of background color/gradient.
      upstream_issue: null
      drop_when: null

    - file: src/main/res/values/colors.xml
      intent: Update launcher_background color from KDE Connect blue to Soduto green
      conflict_prone: false
      resolution_strategy: Keep ours. If upstream adds new colors, incorporate them but keep our launcher_background value.
      upstream_issue: null
      drop_when: null

    - file: "src/main/res/drawable-{hdpi,mdpi,xhdpi,xxhdpi}/{ic_notification,icon}.png"
      intent: Replace KDE Connect notification and app icons (all densities) with Soduto icons
      conflict_prone: false
      resolution_strategy: Keep ours. Binary replacements. Always take our version.
      upstream_issue: null
      drop_when: null

    - file: "src/main/res/mipmap-{hdpi,mdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher{,_round}.png and mipmap-xhdpi/ic_launcher_banner.png"
      intent: Replace KDE Connect launcher and TV banner icons (all densities) with Soduto icons
      conflict_prone: false
      resolution_strategy: Keep ours. Binary replacements. Always take our version.
      upstream_issue: null
      drop_when: null

  deleted:
    - file: "src/main/res/drawable-v24/ic_launcher_{foreground,monochrome,banner_foreground,banner_background}.xml"
      intent: Moved to drawable/ (no v24 qualifier needed); all four replaced with Soduto artwork
      conflict_prone: false
      resolution_strategy: >-
        These files no longer exist in our tree. If upstream modifies them, ignore.
        Our replacements live in src/main/res/drawable/ without the -v24 qualifier.

    - file: src/main/res/drawable-xxxhdpi/ic_notification.png
      intent: Removed — xxxhdpi density not needed; vector covers API 24+ devices
      conflict_prone: false
      resolution_strategy: >-
        File no longer exists in our tree. If upstream adds it, delete it again.
        The vector in drawable-anydpi-v24/ handles all modern devices.

    # 41 upstream locale string files removed; Soduto maintains only 6 languages
    - file: "src/main/res/values-{ar,ast,az,bg,bs,ca,cs,da,el,en-rGB,eo,es,et,eu,fi,ga,gl,hu,ia,id,in,is,it,iw,ka,lt,nl,nn,pl,pt,pt-rBR,ro,sk,sl,sr,sv,ta,tg,tr,uk,zh-rTW}/strings.xml"
      intent: 41 upstream locale folders removed — Soduto cannot maintain this many translations
      conflict_prone: false
      resolution_strategy: >-
        These files no longer exist in our tree. If upstream modifies them,
        ignore. We only maintain de, fr, ja, ko, ru, zh-rCN.

    - file: po/
      intent: Removed entire po/ directory — KDE Weblate/Lokalize sources not used by Soduto's build or F-Droid pipeline
      conflict_prone: false
      resolution_strategy: >-
        This directory no longer exists in our tree. If upstream adds new .po
        files, delete them again on rebase.

    # 19 upstream fastlane locale listing folders removed
    - file: "fastlane/metadata/android/{ar,az-AZ,bg,ca,cs-CZ,en_GB,es-ES,eu-ES,fi-FI,ga,gl-ES,it-IT,iw-IL,ka-GE,nl-NL,no-NO,pl-PL,pt-PT,pt_BR,ro,sk,sl,sv-SE,tr-TR,uk,zh_TW}/"
      intent: 26 upstream fastlane locale listing folders removed — kept only en-US + our 6 translated languages
      conflict_prone: false
      resolution_strategy: >-
        These directories no longer exist in our tree. If upstream adds or
        modifies listings for these locales, delete them again on rebase.

  added:
    - file: src/main/java/org/kde/kdeconnect/plugins/notifications/SodutoNotificationsHelper.kt
      intent: >-
        Notification icon extraction helper — 3-level hierarchy (large, launcher, small)
        with runtime SharedPreferences controlling which levels are active, master
        on/off toggle, and adaptive icon compositing mode (layered vs device shape)

    - file: src/main/java/org/kde/kdeconnect/plugins/notifications/SodutoNotificationFilterController.kt
      intent: >-
        Owns all notification icon row UI logic (two-target row, bottom sheet, master/child
        toggle sync) extracted from NotificationFilterActivity to minimise upstream file changes

    - file: src/main/res/layout/bottom_sheet_notification_icon_settings.xml
      intent: Bottom sheet layout for configuring notification icon hierarchy levels and rendering style

    - file: src/main/res/xml-v31/remotecommandplugin_widget.xml
      intent: API 31+ variant of widget provider — adds previewLayout for live widget picker preview and targetCellWidth/Height for default grid size

    - file: .github/workflows/sync-upstream.yml
      intent: Automated upstream ref syncing via GitHub Actions

    - file: .github/workflows/notify-upstream.yml
      intent: Notifies via GitHub issue when upstream has new commits to integrate

    - file: PATCH.yaml
      intent: Patch (Downstream) manifest (current state, patch inventory, rebase snapshot)

    - file: SYNC_LOG.yaml
      intent: Full rebase history with orphaned commit hashes for recovery

    - file: CLAUDE.md
      intent: AI assistant context for working with this codebase

    - file: AssetIcon.png
      intent: Soduto icon asset used as GitHub README header logo

    - file: src/main/ic_launcher-playstore.png
      intent: 512×512 Play Store listing icon generated by Android Studio Image Asset

    - file: src/main/res/drawable/ic_launcher_foreground.xml
      intent: Soduto adaptive icon foreground (diagonal lines artwork)

    - file: src/main/res/drawable/ic_launcher_monochrome.xml
      intent: Soduto monochrome icon layer for themed icons (Android 13+)

    - file: src/main/res/drawable/ic_launcher_banner_foreground.xml
      intent: Soduto Android TV banner foreground

    - file: src/main/res/drawable/ic_launcher_banner_background.xml
      intent: Soduto Android TV banner background with green gradient

    - file: src/main/res/drawable-anydpi-v24/ic_notification.xml
      intent: Soduto notification icon as vector drawable (API 24+)

    - file: src/main/java/org/kde/kdeconnect/plugins/remotekeyboard/SodutoRemoteKeyboardView.kt
      intent: Material 3 control strip replacing the deprecated KeyboardView in the Remote Keyboard IME

    - file: src/main/res/layout/soduto_remote_keyboard_input_view.xml
      intent: Material 3 icon button strip layout for the Remote Keyboard IME control bar

    - file: src/main/res/values/strings_soduto.xml
      intent: Soduto-specific string additions isolated from upstream strings.xml to avoid rebase conflicts

    - file: protocol.md
      intent: KDE Connect protocol reference (generated from JSON Schemas) for use during plugin development
